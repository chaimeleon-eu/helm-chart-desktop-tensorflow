
The application will be accesible via Guacamole in the following URL:
  {{ .Values.guacamoleConnectionCreation.guacamoleUrl }}
 
There should be a connection created for that desktop with the following name: 
  {{- include "desktop-tensorflow.connectionName" . }}

Once in the desktop, there will be two directory links in /home/chaimeleon/
  - datasets
  - persistent-home

VNC PASSWORD: "{{ .Values.user.password }}"
CONTAINER PASSWORD: "{{ .Values.user.password }}"