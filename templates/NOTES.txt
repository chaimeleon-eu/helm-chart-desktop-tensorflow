
The application will be accesible via Guacamole in the following URL:
  {{ .Values.guacamoleConnectionCreation.guacamoleUrl }}
 
There should be a connection created for that desktop, you can find it by date or the following name: 
  {{ .Values.name }}

Once in the desktop, there will be two directory links in /home/{{ .Values.user.username }}/
  - dataset
  - persistent-home

