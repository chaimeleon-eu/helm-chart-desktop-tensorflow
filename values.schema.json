{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "requestGPU": {
      "type": "boolean",
      "title": "Request GPU",
      "description": "Mark to ensure the workstation has GPU assigned",
      "form": true
    },
    "datasets_list": {
      "type": "string",
      "title": "Dataset list",
      "description": "A comma separated list of the datasets that will be available at the container",
      "form": true
    }
  }
}
